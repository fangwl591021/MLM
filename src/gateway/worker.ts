/*
 * Reference TypeScript module copied from wp-line-point-migrator for the
 * KLINK customer-service Worker. The deployed KLINK Worker is still
 * worker/worker.js, where these routes are wired in JS to avoid adding a build
 * step during the first integration.
 *
 * Live routes in worker/worker.js:
 * - /admin/crm
 * - /admin/crm/members
 * - /admin/crm/sync-members
 * - /admin/crm/sync-points
 * - /admin/points/*
 * - /line-webhook/oa1
 * - /line-webhook/oa2
 */

export {};
